name: Shell commands

on: [push]

jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World from Gtihub Actions"
      - name: Multiline script
        run: |
          node -v
          npm -v
